## Security / 密钥安全

### 不要提交任何密钥
- 不要提交 `.env`、云厂商 Key、Token、Cookie、个人信息等。
- 本仓库已在 `.gitignore` 忽略 `.env`（仅保留 `.env.example`）。

### 如果你不小心提交过密钥
1. 立刻去云厂商控制台 **禁用/轮换**该密钥
2. 删除公开内容并重写历史（如需），再重新生成新 Key

